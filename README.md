# coc-proxy

Run with:

    python3.5 proxy.py

## Installation

Install `twisted` with:

    pip install twisted

Install `pynacl` with:

    pip install pynacl

Install `pyblake2` with:

    pip install pyblake2
